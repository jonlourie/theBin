import styles from "@/styles/Home.module.css";

export default function PortfolioTitle() {
    return (
        <>
        <section className={styles.portfolioComponent_section}>
            <section>Search Third-Party Custodians</section>
        </section>
        <section>

        </section>
        </>
    )
}